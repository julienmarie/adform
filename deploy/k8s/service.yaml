apiVersion: v1
kind: Service
metadata:
  name: adform-landing
  namespace: adform
spec:
  selector:
    app: adform-landing
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
